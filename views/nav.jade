nav(class="navbar #{nav_style}")
	div.container
		div.navbar-header
			button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#navbar", aria-expanded="false", aria-controls="navbar")
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href="/") Trevi 
		if (nav_style !== 'home')
			.search-bar-wrapper
				.search-bar
					form
						input.form-control(type="text", name="region_name", placeholder="서울")
		div#navbar.navbar-collapse.collapse
			ul.nav.navbar-nav
				li.nav-item
					a(href="/admin/users") Admin
				li.nav-item
					a(href="/blogs/main") 이야기
				li.nav-item
					a(href="#contact") Contact
				li.nav-item
					a(href="#", data-toggle="modal", data-target="#signupModal") 회원가입
				if (user)
					li.nav-item.dropdown
						a#userDropdown.dropdown-toggle(role="button", href="#", data-toggle="dropdown", aria-haspopup="true", aria-expanded="true")= user.local.name
							span.caret
						ul.dropdown-menu(aria-labelledby="userDropdown")
							li
								a(href="/users/edit") 정보 수정
							li.divider(role="separator")
							li
								a(href="/logout") 로그아웃
				else
					li.nav-item
						a(href="#", type="button", data-toggle="modal", data-target="#loginModal") 로그인

